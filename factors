#include<stdio.h>
int num,i;
int main()
{
    printf("enter a number");
    scanf("%d",&num);
    printf("factors of %d:\n",num);
    for(i=1;i<=num;i++)
    {
    if(num % i==0)
    {
    printf("%d",i);
    }
    return 0;
    }
}
