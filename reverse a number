// Online C compiler to run C program online
#include <stdio.h>
int i,n,temp,rev;
int main()
{
    printf("enter a digit");
    scanf("%d",&n);
    for(i=1;n>0;i++)
    {
        rev=n%10;
        n=n/10;
        temp=temp*10+rev;
    }
    printf("the no.of digits are %d",temp);
}
